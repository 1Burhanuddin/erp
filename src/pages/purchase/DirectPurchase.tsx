import { PageLayout, PageHeader } from "@/components/layout";

const DirectPurchase = () => {
    return (
        <PageLayout>
            <PageHeader title="Direct Purchase" description="manage direct purchases" />
            <div className="p-4">Direct Purchase Content</div>
        </PageLayout>
    );
};

export default DirectPurchase;
